/**
 * Type exports for Chart IR
 * These types are inferred from Zod schemas and can be used directly.
 */

export type {
  ColumnType,
  Column,
  Dataset,
  Style,
  Geometry,
  CoordinateSystem,
  Encode,
  Mark,
  AxisType,
  AxisPosition,
  Axis,
  LegendPosition,
  Legend,
  TooltipTrigger,
  Tooltip,
  Grid,
  ChartIR,
} from "./schema/index.js";
